from .object_structure import *


class sponsored_product(campaign_id)
	def __init__(self)
		super(sponsored_product, self).__init__()
		self.bidding_strategy=bidding_strategy
		self.top_of_search=top_of_search
		self.product_page=product_page
		self.rest_of_page=rest_of_page

class sponsored_brand(campaign_id)
	def __init__(self)
		super(sponsored_brand, self).__init__()
		self.top_of_search=top_of_search
		self.other_placement=other_placement

class sponsored_display(campaign_id)
	pass
	#not supported at this stage 

	#if enough data compute value, otherwise take account default